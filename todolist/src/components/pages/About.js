import React from "react";

export default function About() {
    return (
        <React.Fragment>
            <p>This is the todolist at version 1.0.0. Part of a crash course.</p>
        </React.Fragment>
    );
}